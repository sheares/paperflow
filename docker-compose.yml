services:
  paperflow:
    platform: linux/amd64   # judging VM architecture
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - path: .env
        required: false
